import { style } from "@vanilla-extract/css";

export const wrapperStyle = style({
  display: "flex",
  flex: 1,
  alignItems: "center",
});

export const inputStyle = style({
  flex: 1,
  margin: "10px",
});
